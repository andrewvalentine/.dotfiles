[alias]
	dsf = "!f() { [ -z \"$GIT_PREFIX\" ] || cd \"$GIT_PREFIX\" && git diff --color \"$@\" | diff-so-fancy | less --tabs=4 -RFX; }; f"
	gst = git status
[core]
	pager = diff-so-fancy | less --tabs=4 -RFX
[user]
	bane = Andrew Valentine
	email = andrew.r.valentine@gmail.com
